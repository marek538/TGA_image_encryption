# TGA Image Cryptography (OpenSSL)

## 📌 Project Description
A C++ utility designed for secure encryption and decryption of TGA image files using symmetric block ciphers. The project leverages the **OpenSSL (`libcrypto`)** library, specifically the high-level `EVP` interface, to provide robust and flexible cryptographic operations.
